{
  "CSS Global for Vite": {
    "prefix": "csg",
    "body": [
      "declare module '*.css?raw' { const content: string; export default content; }"
    ],
    "description": "Adds a global declaration to use ?raw in a Vite package."
  },
  "Console Log": {
    "prefix": "clog",
    "body": ["console.log($0)"],
    "description": "None needed"
  }
}
